# Local development variables for the CollectWise backend service
PORT=4000
# Replace with your Vercel deployment origin when available (e.g., https://collectwise-frontend.vercel.app)
FRONTEND_ORIGIN=http://localhost:3000
# Required by negotiation agent stories in later iterations
OPENAI_API_KEY="sk-..."
